import { ModelProps } from "components/form-fields/types";

export const optionModel: ModelProps = {
  fieldKey: {
    fieldKey: "fieldKey",
    name: "Field Key",
    type: "TEXT",
  },
  name: {
    fieldKey: "name",
    name: "Name",
    type: "TEXT",
  },
};
